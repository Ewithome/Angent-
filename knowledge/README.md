# 建筑规范知识库

把规范文件放到本目录后，智能体会自动检索其中内容来回答问题。

支持的文件格式：

- PDF（`.pdf`）
- Word（`.docx`）
- Markdown（`.md`）
- 纯文本（`.txt`）

示例文件结构：

```text
knowledge/
├── README.md
├── 民用建筑设计统一标准.pdf
├── 建筑设计防火规范.docx
└── 住宅设计规范.md
```

文件放入后不需要重启，智能体下一次检索时会自动重新建立索引。规范原文只保存在本地，不会上传到 GitHub。
